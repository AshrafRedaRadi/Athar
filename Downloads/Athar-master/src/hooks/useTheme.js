import { useTheme } from '../context/ThemeContext';

/**
 * useTheme re-exported from the hooks directory for convenience.
 *
 * Usage:
 *   import { useTheme } from '../hooks/useTheme';
 *   const { isDark, toggleTheme } = useTheme();
 */
export { useTheme };
